/* ============================================================
   STEP — 接続設定
   ------------------------------------------------------------
   ここに Supabase の情報を入れるとチーム全員でデータを共有する
   「共有モード」になります。空のままだと、いままでどおり
   自分の端末だけに保存する「ローカルモード」で動きます。

   入れる値は Supabase の
     Project Settings → Data API（旧 API）
   にある2つです。
     ・Project URL          → SUPABASE_URL
     ・anon public のキー    → SUPABASE_ANON_KEY

   anon キーはブラウザから見える前提の公開キーです。
   実際のアクセス制御は Supabase 側の RLS（supabase/schema.sql）で
   かけているので、ここに貼って問題ありません。
   service_role のキーは絶対に貼らないでください。
   ============================================================ */
window.STEP_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',

  /* インターン本人のログインに使う内部的なメールアドレスのドメイン。
     実際にメールを送ることはありません（Supabase側でメール確認をオフにします）。
     Supabase がこのドメインを弾く場合だけ、別の文字列に変えてください。 */
  AUTH_EMAIL_DOMAIN: 'step-app.local',

  /* 何日チェックが動いていなければ「停滞」として管理者画面で拾うか */
  STALE_DAYS: 14,

  /* 標準期間を何ヶ月超えたら「遅れ」として警告色にするか */
  DELAY_ALERT_MONTHS: 1
};
