# STEP セットアップ手順

このアプリは2つの画面でできています。

| URL | 誰が使う | 何をする |
|---|---|---|
| `/`（`index.html`） | インターン本人 | 自分のキャリアステップにチェックを入れる。ULからの共有事項・テスト点数・振り返りを見る／書く |
| `/admin.html` | UL・メンター | 全員の現状を一覧で見る。申し送りを残す。テスト点数を取り込む。振り返りにコメントを返す |

データの置き場所は **Supabase** です。`shared/config.js` に接続情報を入れると、
全員のデータが1か所に集まる**共有モード**になります。

> **設定前でも壊れません。**
> `shared/config.js` が空のあいだは、これまでどおり自分の端末だけに保存する
> **ローカルモード**で動きます。すでにアプリを使っている人のデータもそのまま残ります。
> `admin.html` はデモデータで動くので、設定前に画面の使い勝手を確認できます。

---

## 全体の流れ

1. Supabase のプロジェクトを作る（5分）
2. `supabase/schema.sql` を実行する（1分）
3. メール確認をオフにする（1分）
4. 接続情報を `shared/config.js` に貼る（1分）
5. パスコードと管理者キーを決める（SQL 1行・2分）
6. デプロイする
7. URLとパスコードをメンバーに共有する ← あとは各自が登録します

**60名分を誰かが入力する作業はありません。**
各メンバーがアプリを開いて自分の情報（氏名・Unit・UL・メンター・入社日）を
登録すると、その内容がそのまま管理者画面の一覧に積み上がっていきます。

---

## 1. Supabase のプロジェクトを作る

1. https://supabase.com にログインし、**New project** を作成
2. リージョンは `Northeast Asia (Tokyo)` を選ぶ（日本から使うので速い）
3. Database Password は控えておく（今回は使いませんが、なくすと面倒です）

無料プランで足ります。60名 × 200項目でもデータ量はごくわずかです。

---

## 2. スキーマを作る

1. Supabase の左メニュー **SQL Editor** を開く
2. このリポジトリの `supabase/schema.sql` の中身を全部コピーして貼り付ける
3. **Run** を押す

`Success. No rows returned` と出れば完了です。
何度実行しても壊れないように書いてあるので、あとで貼り直しても大丈夫です。

これで次のテーブルとアクセス制御ができます。

| テーブル | 中身 |
|---|---|
| `members` | 名簿（名前・Unit・UL・メンター・入社日・認定グレード・権限） |
| `progress` | チェックが入った項目 |
| `member_state` | 相棒の名前・目標・五箇条・アタリマエ・個別設定 |
| `notes` | 申し送り・メモ・面談記録・**昇格面談メモ** |
| `quiz_scores` | ラーニングボックス等のテスト結果 |
| `reviews` | 隔週YWT・月次振り返り・次月目標・ULコメント |
| `app_config` | 共通パスコードと管理者キー（ハッシュ。クライアントからは読めません） |

アクセス制御（RLS）はこうなっています。

- **本人**は自分の行だけ読み書きできる
- **UL / 管理者**は全員分を読み書きできる
- 申し送りを「管理者のみ」で書くと、**本人のブラウザからは取得すらできない**
- 本人が自分の権限（role）やULコメントを書き換えることはできない（そこだけ関数経由に絞っています）
- **本人は自分の行を新しく作れる**（自己登録）。ただし `role` は `member` 固定で、
  共通パスコードが合っていないと作れない

---

## 3. メール確認をオフにする

インターン本人のログインは「名前を選ぶ＋部署共通のパスコード」です。
内部的にはメールアドレスの形をしたIDを使いますが、実際にメールは送りません。
そのため、確認メールを必須にする設定を切っておく必要があります。

1. 左メニュー **Authentication → Sign In / Providers → Email**
2. **Confirm email** を **オフ**
3. **Save**

同じ画面で **Allow new users to sign up** は **オン**のままにしてください。
メンバーが自分で登録するときにアカウントを作る仕組みなので、これがオフだと登録できません。

---

## 4. 接続情報を貼る

1. 左メニュー **Project Settings → Data API**
2. **Project URL** と **anon public** のキーをコピー
3. `shared/config.js` を開いて貼る

```js
window.STEP_CONFIG = {
  SUPABASE_URL: 'https://xxxxxxxxxxxx.supabase.co',
  SUPABASE_ANON_KEY: 'eyJhbGciOi...',
  ...
};
```

`anon` キーはブラウザから見える前提の公開キーなので、ここに貼って問題ありません。
実際の制御は手順2で入れた RLS がしています。
**`service_role` のキーは絶対に貼らないでください。**（これは全権限のキーです）

---

## 5. パスコードと管理者キーを決める

2つの合言葉を決めて、**SQL Editor で1回だけ**実行します。

| 合言葉 | 誰に配る | 何に使う |
|---|---|---|
| **共通パスコード** | メンバー全員 | 登録するとき／次回以降ログインするとき |
| **管理者キー** | UL・メンターだけ | 自分を管理者に昇格させるとき |

```sql
update public.app_config
   set team_passcode  = crypt('ここに共通パスコード', gen_salt('bf')),
       admin_passcode = crypt('ここに管理者キー',     gen_salt('bf')),
       updated_at = now()
 where id = 1;
```

- どちらも**ハッシュにして保存**されるので、あとから中身を見ることはできません。
  変えたいときは同じSQLをもう一度実行してください
- パスコードは全員に配るので、推測されにくい12文字以上をおすすめします
- **管理者キーは、それを知っている人が誰でも管理者になれる**＝全員の申し送りを
  読めるようになる、ということです。メンバーに配らないでください。
  長いランダム文字列にしておくのが安全です

> **この手順を飛ばすと**、URLを知っている人が誰でも登録できてしまいます。
> 未設定のあいだは管理者画面のダッシュボードに赤い警告が出ます。

誰かが意図せず管理者になっていないかは、管理者画面の
**キャリア現状一覧 → 絞り込みを「UL・管理者のみ」** にすればいつでも確認できます。
戻したいときは SQL Editor で
`update public.members set role='member' where name='...';` を実行します。

---

## 6. デプロイする

このリポジトリは静的ファイルだけなので、ビルドは不要です。
Vercel に接続しているなら、`main`（または対象ブランチ）に push すればそのまま公開されます。

```
index.html            インターン本人の画面
admin.html            UL・メンターの画面
shared/config.js      Supabaseの接続情報（ここだけ編集する）
shared/steps.js       キャリアステップの定義と進捗の計算
shared/ui.js          共通の見た目パーツ・日付やCSVの整形
shared/store.js       データの読み書き（Supabase / ローカル）
supabase/schema.sql   Supabaseに貼るSQL
```

キャリアステップシートの項目を変えたいときは `shared/steps.js` の `GRADES` を編集します。
本人の画面と管理者画面が同じ定義を読むので、片方だけズレることはありません。

> **項目を編集するときの注意**
> チェックの保存キーは「グレード番号・セクションの順番・項目の順番」から作られています。
> 途中に項目を**挿入**すると、それ以降のチェックが1つずれます。
> 文言の修正は安全ですが、項目の追加は**セクションの末尾に足す**のが安全です。

---

## 7. URLとパスコードを共有する（ここから先は各自が登録します）

メンバーに伝えるのはこの2つだけです。

```
アプリのURL：https://intern-career-path.vercel.app/
共通パスコード：（手順5で決めたもの）
```

### メンバーがすること（初回・3分）

1. URLを開く
2. **「はじめる（新規登録）」** を押す
3. 氏名・所属Unit・UL・メンター・入社日・いま認定されているGrade を入れる
   - Unit と UL は、**すでに誰かが登録した表記が選択肢に出ます**。
     選べば表記が揃うので、集計が分かれません。最初の人だけ手で入力します
   - 入社日を入れると**昇格予定時期が自動で計算されます**
   - わからない項目は空でもかまいません。あとから「マイシート」で直せます
4. 共通パスコードを入れて **登録してはじめる**

登録した瞬間から、その人は管理者画面の一覧に出ます。
**60名分を誰かがまとめて入力する必要はありません。**

### メンバーがすること（2回目以降）

URLを開いて、**一覧から自分の名前を選び、同じパスコードを入れる**だけです。
（一覧には、すでに登録した人の名前が並びます）

### UL・メンターがすること

1. メンバーと同じ手順で、まず自分を登録する
2. **マイシート**タブをひらき、いちばん下の **「管理者の方へ」** に
   手順5で決めた**管理者キー**を入れて **管理者になる** を押す
3. そのまま管理画面に移動します。次回からは `/admin.html` を直接ひらけます

Supabase の画面を触る必要はありません。

### 登録状況の見えかた

管理者画面のダッシュボードに **登録状況** カードがあり、次が分かります。

- 登録済み人数、直近7日の新規登録
- **情報が足りない人**（Unitか入社日が空のまま登録した人）
- 最近登録した人の一覧

入社日が空だと昇格予定を計算できないので、そこだけは埋めてもらってください。
本人にマイシートで入れてもらっても、管理者がメンバー詳細の**設定**タブで
入れてもかまいません。

---

## 8. 前提として共有しておいてほしいこと

インターン本人のログインは **名前を選ぶ → 共通パスコード** です。

- 初回に登録したパスコードが、そのままその人のパスワードになります
- 名前を選ぶだけなので、**パスコードを知っている人は理論上ほかの人の名前でも入れます**。
  厳密な本人認証ではありません
- だから、本人に見せたくない内容は申し送りの公開範囲を「**管理者のみ**」に
  してください。そちらは Supabase 側で遮断されるので、本人の名前でログインしても
  取得できません

より厳しくしたい場合は、メールでのログイン（マジックリンク）や Google 連携に
差し替えられます。`supabase/schema.sql` はそのままで動きます。

### 名簿も隠したい場合

ログイン画面に名前の一覧を出すため、`member_roster` というビューだけは
ログイン前でも読めるようにしています（出るのは名前・Unit・UL名だけ）。
これも隠したい場合は次のどちらかを取ってください。

- Vercel の **Deployment Protection**（パスワード）をサイト全体にかける
- SQL Editor で `revoke select on public.member_roster from anon;` を実行し、
  ログイン画面を「名前を選ぶ」から「自分のIDを打つ」形に変える

### 先に名簿を作っておきたい場合（任意）

「まだ登録していない人」を把握したいときは、先に行だけ作っておけます。
`auth_id` が空の行は、その名前を選んでパスコードを入れた人と紐付きます。
自己登録とどちらでも動くので、混在していても問題ありません。

```sql
insert into public.members (name, slug, unit, ul, join_date, role) values
  ('山田 太郎', 'yamada-taro', 'Unit A', '佐藤 花子', '2026-04-01', 'member')
on conflict (slug) do nothing;
```

`slug` はログイン用の内部IDです。半角英数とハイフンだけを使ってください
（自己登録した人には自動でランダムなIDが振られます）。

---

## 9. ラーニングボックスのテスト点数を取り込む

1. クイズの管理画面（https://orient-quiz.vercel.app/admin.html）から結果をCSVで書き出す
2. `/admin.html` → **テスト点数** → **CSVを取り込む**

読める形式は次のとおりです。

- ヘッダー行がある場合：`名前`／`テスト名`／`点数`／`満点`／`受験日` を含む列名を自動で探します
  （`氏名` `得点` `正答数` `問題数` `日付` などの言い方も拾います）
- ヘッダー行がない場合：`名前, テスト名, 点数, 満点, 受験日` の順として読みます

補足：

- 名前は空白を無視して名簿と突き合わせます。見つからない名前は飛ばして、最後に一覧で知らせます
- **同じ（名前・テスト名・受験日）の行は上書きされる**ので、同じCSVを何度取り込んでも増えません
- 満点があれば正答率を計算し、9割以上を合格として色分けします
- 1件だけなら **＋ 手で追加** から直接入れられます

将来クイズ側から直接書き込みたくなったら、`quiz_scores` テーブルに
`member_id / quiz_name / score / max_score / taken_on / source` を入れるだけです。
本人の画面にも管理者画面にも自動で出ます。

---

## 10. できること早見表

### インターン本人（`/`）

- キャリアステップのチェック（これまでどおり）
- **記録**タブ
  - **共有事項**：ULやメンターが残した申し送り・面談メモ（「管理者のみ」のものは出ない）
  - **テスト**：自分のテスト点数の履歴と平均正答率
  - **振り返り**：隔週YWT（Y／W／T）と月次振り返り・次月目標の提出。ULコメントも読める
- ホームに直近の共有事項が2件出る

### UL・メンター（`/admin.html`）

| 画面 | 見られるもの |
|---|---|
| ダッシュボード | 遅れ・停滞・昇格対象・未提出の人数、グレード分布、Unitごとの状況、いま見るべき人、直近の申し送り |
| キャリア現状一覧 | 全員 × Grade 1〜10 のヒートマップ、在籍月数、認定G／完了G、昇格予定、標準期間との差、最終更新、CSV書き出し |
| 昇格と詰まり | 昇格面談の対象者、昇格予定時期の月別タイムライン、詰まっている項目のランキング、カテゴリ別のつまずき |
| 申し送り・メモ | 全員横断のタイムライン、種別（メモ／申し送り／面談／**昇格面談**／エスカレ／称賛）、公開範囲の切り替え、CSV書き出し |
| テスト点数 | メンバー × テストのマトリクス、テストごとの受験率と平均、CSV取込／書き出し |
| YWT・月次 | 提出状況のマトリクス、次月目標の一覧、ULコメントの返信 |
| リーダーMTG | Unitごとの1枚サマリ（進捗・昇格予定・詰まり・提出状況・次月目標）、テキストコピー、印刷／PDF |

メンバーの名前を押すと右からパネルが開き、その人の
グレード別達成状況・未達項目（**その場でチェックを入れられます**）・アタリマエ評価・
申し送り・テスト・振り返り・名簿情報（昇格予定の上書きを含む）をまとめて扱えます。

---

## 11. 判定のルール

管理者画面のアラートは次の基準で出しています。`shared/config.js` で調整できます。

| 表示 | 条件 |
|---|---|
| **昇格対象** | 完了グレード数 ≧ 認定グレード（＝いま認定されているグレードの項目を終えている） |
| **遅れ** | 在籍月数 − そのグレードの標準期間 ≧ `DELAY_ALERT_MONTHS`（既定 1ヶ月） |
| **動きなし** | 最後にチェックが入ってから `STALE_DAYS` 日以上（既定 14日）。記録が1件もない場合も含む |
| **YWT未提出** | 今の期間（1〜15日／16日〜）のYWTが未提出 |
| **月次未提出** | 今月の振り返りが未提出 |
| **アタリマエ✕** | 本人の自己申告に✕がある |

「昇格予定」は `promotion_target` が入っていればそれを、空なら
**入社日 ＋ 挑戦中グレードの標準期間**（`GRADES[].period` の1ヶ月・2ヶ月…）を使います。
個別に前後させたいときは、メンバー詳細の**設定**タブから日付を上書きしてください。

---

## 困ったとき

| 症状 | 見るところ |
|---|---|
| `Supabaseの「Confirm email」をオフにしてください` と出る | 手順3をやり直す |
| `パスコードが違います` と出るが合っているはず | その人が**初回**に登録したパスコードと違う可能性があります。Authentication → Users から該当ユーザーを消せば、次のログインで再登録されます |
| 管理者画面が「管理者権限がありません」になる | `members` の該当行の `role` が `ul` / `admin` か、`auth_id` が正しく紐付いているかを確認（手順6） |
| ログイン画面に名前が出ない | `members` に行があるか、`active` が `true` か。手順2のSQLを流し直すと `member_roster` が作り直されます |
| 「デモデータを表示しています」が消えない | `shared/config.js` の2つの値が空のままです（手順4） |
| CSVの名前が全部飛ばされる | `members.name` の表記（スペースの有無・旧字）と突き合わせています。空白は無視されるので、漢字が違う人だけ名簿側を直してください |
| 進捗の数字が本人と管理者でズレる | 計算は `shared/steps.js` の1か所だけです。ブラウザのキャッシュを消して再読み込みしてください |
| 登録しようとすると `パスコードが違います` | 手順5で設定したものと違います。SQLをもう一度実行して設定し直せます |
| `Allow new users to sign up` 系のエラーで登録できない | 手順3でサインアップをオンにしてください |
| 同じ人が2行できてしまった | 2回目の登録で別のログインを作った場合に起こります。管理者画面のメンバー詳細で使わない行の名前を変え、SQL Editor で `update public.members set active=false where id='...';` とすれば一覧から消えます |
| 管理者になれない（キーが違うと出る） | 手順5の `admin_passcode` が未設定か、キーが違います。未設定の場合はその旨のメッセージが出ます |
| 意図しない人が管理者になっている | 管理者キーが漏れています。手順5のSQLでキーを変え、`update public.members set role='member' where name='...';` で戻してください |
